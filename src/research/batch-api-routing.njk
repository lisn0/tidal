---
layout: "research.njk"
permalink: "research/batch-api-routing.html"
titleCore: "Batch API routing: 50% off for the work that can wait"
description: "How to route evals, enrichment, and nightly LLM jobs to Batch APIs while keeping latency-sensitive paths on synchronous models."
slug: "batch-api-routing"
schemaHeadline: "Batch API routing: 50% off for the work that can wait · LLM CFO"
schemaDescription: "OpenAI and Anthropic Batch APIs cut input+output costs by ~50% in exchange for a 24-hour SLA. What's eligible, what isn't, and how to route eval, enrichment, and nightly jobs to batch without touching your latency-sensitive paths."
breadcrumbName: "Batch API routing: 50% off for the work that can wait"
datePublished: "2026-04-25"
dateModified: "2026-07-12"
copyrightSymbol: "©"
footerSeparator: "·"
faqJsonLd: |-
  {"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What discount do Batch APIs provide?","acceptedAnswer":{"@type":"Answer","text":"Batch APIs provide approximately 50% off both input and output tokens in exchange for a 24-hour SLA. This applies to OpenAI Batch, Anthropic Message Batches, Bedrock batch inference, and Vertex batch prediction."}},{"@type":"Question","name":"What workloads should you route to batch?","acceptedAnswer":{"@type":"Answer","text":"Route eval pipelines, data enrichment, content generation at scale, nightly summarization, embeddings backfills, and synthetic data preparation to batch. These are workloads where humans don't need results immediately and latency is not a constraint."}},{"@type":"Question","name":"What should not go to batch?","acceptedAnswer":{"@type":"Answer","text":"Avoid batch for user-facing endpoints, interactive agents, tool-calling loops where one call's output feeds the next, and anything tied to a webhook with a tight timeout. Batch's 24-hour SLA means results could arrive anywhere between 8 minutes and 22 hours."}},{"@type":"Question","name":"How do you migrate workloads to batch?","acceptedAnswer":{"@type":"Answer","text":"Start by pulling 30 days of usage by endpoint sorted by spend. Ask the owner of each high-spend endpoint whether 24 hours later would be acceptable. Migrate one endpoint at a time with a fallback to sync, then reconcile the next invoice to confirm the discount landed."}},{"@type":"Question","name":"What are common batch pitfalls?","acceptedAnswer":{"@type":"Answer","text":"Rate limits are separate from sync so per-batch token caps still apply. Cost reports don't auto-attribute batch lines to cost centers unless tagged in metadata. Cache hits don't compound with batch discounts on every provider. Output token estimates are best-guess; set reasonable max_tokens ceilings to prevent one runaway request from holding up the entire batch."}}]}
---
  <div class="eye">RESEARCH · TECHNIQUE</div>
  <h1 class="page-title">Batch API <em>routing.</em></h1>
  <p class="updated">25 April 2026</p>
  <p class="byline">By the LLM CFO team</p>

  <p class="lede">The Batch API is the cheapest dollar-for-dollar lever in the playbook: a flat ~50% off both input and output tokens in exchange for a 24-hour completion window. The only reason teams don't use it is they haven't audited which of their workloads are actually latency-sensitive.</p>

  <h2>What you get</h2>
  <table class="kv">
    <thead><tr><th scope="col">Batch surface</th><th scope="col">Discount mechanics</th></tr></thead>
    <tbody>
      <tr><td>OpenAI Batch</td><td>~50% off input + output · 24-hour SLA · JSONL upload · most chat/completions/embeddings models</td></tr>
      <tr><td>Anthropic Message Batches</td><td>~50% off input + output · 24-hour SLA · up to 100k requests / 256 MB per batch · all current Claude models</td></tr>
      <tr><td>Bedrock batch inference</td><td>~50% off · async S3 in/out · region + model coverage varies</td></tr>
      <tr><td>Vertex batch prediction</td><td>~50% off list price for Gemini · BigQuery or GCS in/out</td></tr>
    </tbody>
  </table>

  <h2>How it actually works</h2>
  <p>You upload a file of requests; the provider runs them when capacity is available; you poll or webhook for the result file. Caching, function calling, and structured outputs are typically supported. Streaming is not ; by definition, batch is async. Failed individual requests come back in an error file alongside the success file.</p>

  <h2>Workloads that should be on batch today</h2>
  <ol>
    <li><strong>Eval pipelines.</strong> Regression tests, LLM-as-judge runs, golden-set scoring. These are the canonical batch case ; you don't care if results land in 3 minutes or 3 hours, you care about the bill.</li>
    <li><strong>Data enrichment.</strong> Tagging, classification, entity extraction over a backlog of records. If it's a one-time job over a million rows, it belongs in batch.</li>
    <li><strong>Content generation at scale.</strong> Bulk product descriptions, alt text, translations, marketing variants. Anything where a human reviews the output later anyway.</li>
    <li><strong>Nightly summarization.</strong> Daily digests, account-level recaps, weekly reports. Schedule the batch to start at 22:00; results are ready before the morning email goes out.</li>
    <li><strong>Embeddings backfills.</strong> Reindexing a corpus, migrating embedding models. Long-tail volume that doesn't need to land synchronously.</li>
    <li><strong>Synthetic data and fine-tune dataset prep.</strong> Generating training pairs, paraphrases, instruction variants.</li>
  </ol>

  <h2>Anti-patterns: do not route to batch</h2>
  <ul>
    <li><strong>User-facing endpoints.</strong> Anything where a human is waiting on a screen.</li>
    <li><strong>Interactive agents and chat.</strong> The 24-hour SLA is a contract, not a soft target ; your batch could land in 8 minutes or 22 hours.</li>
    <li><strong>Tool-calling loops</strong> where one call's output feeds the next call's input. Batch can't do multi-step within a single batch.</li>
    <li><strong>Anything tied to a webhook fan-out</strong> with a tight timeout (Stripe, GitHub, etc.).</li>
    <li><strong>"Nice to have" real-time UX.</strong> If a PM can't articulate why the user needs it in 2 seconds, ask ; but don't assume.</li>
  </ul>

  <h2>Practical migration order</h2>
  <ol>
    <li>Pull last 30 days of usage by endpoint or job-name. Sort by spend.</li>
    <li>For the top 10, ask the owner: "would 24 hours later be fine?" In our audits, the answer is yes for 30–50% of spend.</li>
    <li>Migrate one endpoint at a time. Keep a fallback to sync for the long tail of items where 24 hours is genuinely too long.</li>
    <li>Reconcile the next invoice. Batch line items appear separately; confirm the discount actually landed.</li>
  </ol>

  <h2>Subtleties that bite</h2>
  <ul>
    <li><strong>Rate limits are separate</strong> from sync. You can typically queue more, but the per-batch token caps still apply.</li>
    <li><strong>Cost reports don't auto-attribute</strong> batch lines to the right cost center unless you tag them in metadata.</li>
    <li><strong>Cache hits don't compound with batch discounts</strong> on every provider ; read the small print before modeling savings.</li>
    <li><strong>Output token estimates are best-guess.</strong> Set a reasonable `max_tokens` ceiling; batch failures from one runaway request can hold up the whole file.</li>
  </ul>

  <h2>Related</h2>
  <ul>
    <li><a href="/research/scale-tier-decision">Scale Tier vs Flex vs Batch vs Standard</a></li>
    <li><a href="/research/llm-cost-optimization">Full LLM cost optimization guide</a></li>
    <li><a href="/research/evals-cost-discipline">Evals cost discipline</a></li>
    <li><a href="/research/prompt-caching-explained">Prompt caching explained</a></li>
  </ul>

  <a class="back" href="/">← Back to llmcfo.com</a>
