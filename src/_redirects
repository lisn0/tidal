# Cloudflare Pages redirects
# https://developers.cloudflare.com/pages/configuration/redirects/

# www → apex is handled in worker.js (Workers static-assets _redirects matches
# path only and rejects absolute-URL/host-based sources, so it can't live here).

# Company booking URL. /book is a real page now (src/book.njk); this is the
# "pick a time" link on it. Redirecting /book itself would shadow the page —
# _redirects matches before static assets.
/book/schedule  https://cal.eu/llmcfo  302

# Trailing-slash normalization handled by Cloudflare
